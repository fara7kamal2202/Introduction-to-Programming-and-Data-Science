# ================================
# Exercise: Create a function 'find_max' that takes a list of numbers returns the one that is biggest.
# Call the function and print the result.
# ================================

#Note: You can either use build-in python function or compute the maximum using a loop


def find_max(numbers_list):
    return max(numbers_list)

my_list = [1, 6, 3, 9, 10]
print(find_max(my_list))
